require 'spec_helper'

describe LibChecker do
  it 'has a version number' do
    expect(LibChecker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
